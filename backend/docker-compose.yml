services:
  mcp:
    build: .
    command: python mcp_server/github_mcp_server.py
    env_file:
      - ../.env

  fastapi:
    build: .
    command: uvicorn app.api:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    